<h1 class="text-center my-5">
    {{this.parent.name}}
</h1>

<div class="d-flex justify-content-center">
    <div class="card" style="width: 18rem;">
        <div class="card-body">
            <p class="card-text">Age : {{parent.age}} ans</p>
            <p class="card-text">Enfant : <a href="/student/{{parent.student.id}}">{{parent.student.name}}</a></p>
        </div>
    </div>
</div>